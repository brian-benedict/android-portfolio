import 'package:flutter/material.dart';

class CertifiedSkills extends StatelessWidget {
  const CertifiedSkills({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Certified Skills'),
      ),
    );
  }
}
